# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty: no source files, no commits, and the `origin` remote
(`https://github.com/jaehobyte/Diffuse.git`) has no branches yet. Everything below is the only
information verifiable today.

**When the first code lands, re-run `/init` so this file describes the actual build, test, and
architecture instead of this placeholder.**

## Repository facts

- Working directory: `/home/jaeho/vibe_editor_260905`
- Remote: `origin` → `https://github.com/jaehobyte/Diffuse.git` (empty)
- Default branch: `main` (no commits yet; the first commit establishes it)

## Related prior work

Two sibling directories on this machine hold earlier iterations of the same problem space and are
useful for context, but they are separate repositories — do not assume their code, layout, or
conventions apply here unless told so:

- `/home/jaeho/vibe_edit` — planning and research documents (Korean-language specs, architecture
  write-ups, survey material). Has its own `CLAUDE.md`.
- `/home/jaeho/vibe_edit_poc` — a working proof of concept (`client/`, `server/`, `contracts/`,
  `Makefile`, `docs/`, remote `WebVibeEditor.git`). Has its own `CLAUDE.md`, `README.md`, and
  `DESIGN.md`.

## Commands

None yet — no build system, test runner, or linter is configured in this repository. Add them here
as they are introduced, including how to run a single test.

## Architecture

Not yet established. Document the cross-file structure here once it exists, rather than restating
the directory listing.
